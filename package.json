{
    "name": "wasted-lines-detector",
    "version": "1.0.0",
    "scripts": {
        "prebuild": "npm install",
        "build": "ncc build index.js -o dist",
        "prehook": "npm run build"
    },
    "dependencies": {
        "@actions/core": "^1.10.0",
        "@actions/github": "^5.0.0",
        "node-fetch": "^2.6.1"
    },
    "devDependencies": {
        "husky": "^9.1.7",
        "lint-staged": "^15.4.3"
    }
}
